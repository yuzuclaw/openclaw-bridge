# Example values (INSECURE). For public repo demonstration only.
# DO NOT use these values in a real deployment.

BRIDGE_HOST=0.0.0.0
BRIDGE_PORT=8888

# API access token (Bearer)
BRIDGE_API_KEY=api-key

# Exec signature key (HMAC)
EXEC_SIGNING_KEY=exec-key

# Console BasicAuth
CONSOLE_BASIC_USER=admin
CONSOLE_BASIC_PASS=admin123
